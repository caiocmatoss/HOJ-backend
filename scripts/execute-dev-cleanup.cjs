#!/usr/bin/env node
require("tsx/cjs");
const fs = require("node:fs");
const path = require("node:path");
const { createHash } = require("node:crypto");
const { spawnSync } = require("node:child_process");
const { PrismaPg } = require("@prisma/adapter-pg");
const { PrismaClient } = require("../generated/prisma/client.ts");
const { classifyResidue } = require("../src/catalog/test-residue.ts");
const { manifestHash, relationCount, validateApproval, validateExecutionFlags } = require("../src/catalog/cleanup-plan.ts");
const { executeCleanupTransaction, validateBackupArtifact } = require("../src/catalog/cleanup-execution.ts");
const { resolveDatabaseConfig, assertCatalogDatabase } = require("../src/catalog/database-identity.ts");
const APPROVAL_PATH = path.join(__dirname, "dev-cleanup-approved-20260904.json");
const ACTION = "REMOVE-8-REVIEWED-E2E-FIXTURES";
const EXPECTED = [["User", "cmtcio3wn00006onjt9yqnxpl"], ["User", "cmtcj182q00004onjgr1hwrli"], ["Venue", "cmtci8l4n0004xonjeehvtu2k"], ["Venue", "cmtcif34b0004uknjl5p8i2xt"], ["Venue", "cmtcimlap00046onjvz9nrg5x"], ["Venue", "cmtcio4a400016onj8z6xhhef"], ["Venue", "cmtcj15nc00044onj46ujfie3"], ["Venue", "cmtcj18fu00014onjsav795e2"]].map(([entity, id]) => ({ entity, id }));
function parseDotEnv(filePath) { if (!fs.existsSync(filePath)) return {}; const values = {}; for (const line of fs.readFileSync(filePath, "utf8").split(/\r?\n/)) { const match = line.match(/^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=\s*(.*)\s*$/); if (!match) continue; const value = match[2]; values[match[1]] = (value.startsWith("\"") && value.endsWith("\"") || value.startsWith("'") && value.endsWith("'")) ? value.slice(1, -1) : value; } return values; }
function valueArg(argv, name) { const index = argv.indexOf(name); return index >= 0 ? argv[index + 1] : undefined; }
function parseArgs(argv) { return { manifest: path.resolve(valueArg(argv, "--manifest") || ".tmp/dev-cleanup-manifest.json"), execute: argv.includes("--execute"), confirmDatabase: valueArg(argv, "--confirm-database"), confirmManifestSha: valueArg(argv, "--confirm-manifest-sha"), confirmAction: valueArg(argv, "--confirm-action") }; }
function createPrismaClient(url) { return new PrismaClient({ adapter: new PrismaPg({ connectionString: url }) }); }
function identity(entity, record) { if (entity === "User") return `${record.name} / ${record.email}`; if (entity === "Venue") return `${record.name} / ${record.source ?? ""} / ${record.externalId ?? ""}`; return record.title ?? record.name ?? record.id; }
function relationTotal(counts) { return Object.values(counts ?? {}).reduce((sum, value) => sum + Number(value || 0), 0); }
function postgresEnvironment(databaseUrl) { const url = new URL(databaseUrl); const env = { ...process.env, PGHOST: url.hostname, PGDATABASE: decodeURIComponent(url.pathname.slice(1)), PGUSER: decodeURIComponent(url.username), PGPASSWORD: decodeURIComponent(url.password) }; if (url.port) env.PGPORT = url.port; return env; }
function createBackup(databaseUrl, dependencies = {}) { const directory = path.resolve(".tmp"); fs.mkdirSync(directory, { recursive: true }); const backupPath = path.join(directory, `pre-dev-cleanup-${Date.now()}.dump`); const binary = process.env.PG_DUMP_BIN || "pg_dump"; const spawn = dependencies.spawnSync || spawnSync; const fileSystem = dependencies.fs || fs; const result = spawn(binary, ["--format=custom", "--file", backupPath], { env: postgresEnvironment(databaseUrl), shell: false, encoding: "utf8", maxBuffer: 1024 * 1024 }); if (result.error || result.status !== 0) throw new Error("pg_dump backup failed; cleanup aborted."); const size = fileSystem.existsSync(backupPath) ? fileSystem.statSync(backupPath).size : 0; const validationError = validateBackupArtifact(fileSystem.existsSync(backupPath), size); if (validationError) throw new Error(validationError); return { path: backupPath, size, sha256: createHash("sha256").update(fileSystem.readFileSync(backupPath)).digest("hex") }; }
async function preflight(prisma, manifest) { const idsByEntity = Object.fromEntries(EXPECTED.map((entry) => [entry.entity, EXPECTED.filter((item) => item.entity === entry.entity).map((item) => item.id)])); const liveUsers = await prisma.user.findMany({ where: { id: { in: idsByEntity.User } }, include: { _count: { select: { checkins: true, favorites: true, friendshipsSent: true, friendshipsReceived: true, groupMemberships: true, createdGroups: true, sentInvites: true, receivedInvites: true, messages: true, sentDirectMessages: true, receivedDirectMessages: true, notifications: true, locations: true } } } }); const liveVenues = await prisma.venue.findMany({ where: { id: { in: idsByEntity.Venue } }, include: { _count: { select: { events: true, checkins: true, favorites: true, groups: true, images: true } } } }); const live = { User: liveUsers, Venue: liveVenues }; const manifestByKey = new Map(manifest.records.map((record) => [`${record.entity}:${record.id}`, record])); const blocked = []; for (const entry of EXPECTED) { const record = live[entry.entity].find((item) => item.id === entry.id); const planned = manifestByKey.get(`${entry.entity}:${entry.id}`); if (!record) { blocked.push(`${entry.entity}:${entry.id} missing`); continue; } if (identity(entry.entity, record) !== planned.identity) blocked.push(`${entry.entity}:${entry.id} identity drift`); if (!classifyResidue(entry.entity, record)) blocked.push(`${entry.entity}:${entry.id} fixture evidence drift`); if (relationTotal(record._count) !== 0) blocked.push(`${entry.entity}:${entry.id} has relations`); if (relationCount(planned) !== 0) blocked.push(`${entry.entity}:${entry.id} had manifest relations`); } if (blocked.length) throw new Error(`Preconditions failed: ${blocked.join("; ")}`); return { live, idsByEntity }; }
async function main() { const options = parseArgs(process.argv.slice(2)); const config = resolveDatabaseConfig(process.env, parseDotEnv(path.join(__dirname, "..", ".env"))); assertCatalogDatabase(config.name); if (config.name !== "hojeond") throw new Error(`Refusing cleanup: expected database hojeond, got ${config.name}.`); const buffer = fs.readFileSync(options.manifest); const manifest = JSON.parse(buffer.toString("utf8")); const approval = JSON.parse(fs.readFileSync(APPROVAL_PATH, "utf8")); const hash = manifestHash(buffer); if (approval.database !== "hojeond") throw new Error("Approval database must be hojeond."); if (approval.manifestSha256 !== hash) throw new Error("Manifest SHA-256 does not match approval file."); const errors = validateApproval(manifest, approval.approved, EXPECTED); if (errors.length) throw new Error(errors.join(" ")); const flagErrors = validateExecutionFlags(options.execute, options.confirmDatabase, options.confirmManifestSha, options.confirmAction, hash); if (flagErrors.length) throw new Error(flagErrors.join(" ")); if (options.execute) { const backup = createBackup(config.url); const prisma = createPrismaClient(config.url); try { try { await preflight(prisma, manifest); } catch (error) { throw new Error(`Cleanup aborted; no database writes were made. ${error instanceof Error ? error.message : "unknown error"}`); } console.log(`Backup path: ${backup.path}`); console.log(`Backup SHA-256: ${backup.sha256}`); console.log(`Backup size: ${backup.size}`); const result = await executeCleanupTransaction(prisma, { User: EXPECTED.filter((x) => x.entity === "User").map((x) => x.id), Venue: EXPECTED.filter((x) => x.entity === "Venue").map((x) => x.id) }); console.log("COMMITTED CLEANUP"); console.log(`Users deleted: ${result.users}`); console.log(`Venues deleted: ${result.venues}`); console.log(`Total deleted: ${result.users + result.venues}`); } catch (error) { const message = error instanceof Error ? error.message : "unknown error"; if (message.startsWith("Cleanup aborted;")) throw error; throw new Error(`Cleanup failed. Transaction rolled back. ${message}`); } finally { await prisma.$disconnect(); } return; }
  const prisma = createPrismaClient(config.url); try { const preflightResult = await preflight(prisma, manifest); console.log(`Database source: ${config.source}`); console.log(`Database name: ${config.name}`); console.log("DRY RUN — NO DATABASE WRITES"); console.log(`Manifest path: ${options.manifest}`); console.log(`Manifest SHA-256: ${hash}`); console.log("Approved records: 8"); console.log("Validated: 8"); console.log("Blocked: 0"); for (const entry of EXPECTED) { const record = preflightResult.live[entry.entity].find((item) => item.id === entry.id); console.log(`WOULD DELETE | ${entry.entity} | ${entry.id} | ${identity(entry.entity, record)}`); } console.log("No database changes were made."); } finally { await prisma.$disconnect(); } }
if (require.main === module) main().catch((error) => { console.error(error instanceof Error ? error.message : "Cleanup execution failed."); process.exitCode = 1; });
module.exports = { parseArgs, identity, relationTotal, postgresEnvironment, createBackup, executeCleanupTransaction };